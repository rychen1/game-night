import {
  createGame,
  defaultSettings,
  describeSetup,
  gameTitle,
  LOBBY_MAX_PLAYERS,
  maxPlayersForGame,
  validateSettings,
} from "./registry.ts";

export {
  createGame,
  defaultSettings,
  describeSetup,
  gameTitle,
  LOBBY_MAX_PLAYERS,
  maxPlayersForGame,
  validateSettings,
};
